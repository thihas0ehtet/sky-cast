import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryBlue = Color(0xFF0a6ed1);
  static const Color darkBlue = Color(0xFF084b83);
  static const Color mediumBlue = Color(0xFF125b9e);
  static const Color lightBlue = Color(0xFF6ec9f5);
  static const Color veryLightBlue = Color(0xFF87e0ff);

  // Text colors
  static const Color textColorDark = Color(0xFF093a6a);
  static const Color textColorMedium = Color(0xFF0a4c78);
  static const Color textColorLight = Color(0xFF0b4c8a);

  // Background gradient colors
  static const List<Color> backgroundGradient = [
    Color(0xFF6ec9f5),
    Color(0xFF87e0ff),
    Color(0xFFFFFFFF),
  ];

  // Button colors
  static const Color searchButton = Color(0xFF0a6ed1);
  static const Color backButton = Color(0xFF084b83);

  // Card colors
  static const Color cardBackground = Colors.white;
  static const Color cardShadow = Colors.black12;
}
